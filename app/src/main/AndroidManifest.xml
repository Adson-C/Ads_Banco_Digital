<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdsBancoDigital"
        tools:targetApi="31">
        <activity
            android:name=".deposito.DepositoActivity"
            android:exported="false" />
        <activity
            android:name=".App.SplashActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".autenticacao.RecuperarContaActivity" />
        <activity android:name=".autenticacao.LoginActivity" />
        <activity android:name=".autenticacao.CadastroActivity" />
        <activity android:name=".App.MainActivity" />
    </application>

</manifest>